discord.py>=2.0
aiohttp>=3.8
PyYAML>=6.0
onnxruntime>=1.12
Pillow>=10.0
numpy>=1.23
huggingface_hub>=0.20
# ultralytics (pulls in ~2 GB of torch) is only needed for the one-time
# EraX NSFW ONNX export in download_erax.py. Not required once
# models/erax_nsfw.onnx exists on disk. Install it on demand with:
#     pip install ultralytics
